════════════════════════════════════════════════════════════════════════════════
   📋 COPY-PASTE READY COMMANDS - DevOps Portfolio Deployment
════════════════════════════════════════════════════════════════════════════════

Repository: charan-portfolio
Live URL: https://YOUR_USERNAME.github.io/charan-portfolio/

════════════════════════════════════════════════════════════════════════════════
   🚀 OPTION 1: FULLY AUTOMATED (RECOMMENDED)
════════════════════════════════════════════════════════════════════════════════

cd /Users/charan/Library/CloudStorage/OneDrive-AlignTechnology,Inc/work_align/kafka_repos/kafka_ops_healthcheck_services/web-portfolio && ./auto-deploy.sh

Done! Your portfolio will be live in 2 minutes.

════════════════════════════════════════════════════════════════════════════════
   📝 OPTION 2: MANUAL STEP-BY-STEP
════════════════════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1: Create GitHub Repository
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

gh repo create charan-portfolio --public --description "Professional portfolio - Senior Platform Engineer specializing in Kafka, Cloud, and Data Engineering" --homepage "https://$(gh api user --jq '.login').github.io/charan-portfolio/"

# Creates a public repository with description and homepage URL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2: Navigate to Portfolio Directory
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /Users/charan/Library/CloudStorage/OneDrive-AlignTechnology,Inc/work_align/kafka_repos/kafka_ops_healthcheck_services/web-portfolio

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3: Initialize Git and Add Remote
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git init
git branch -M main
git remote add origin "https://github.com/$(gh api user --jq '.login')/charan-portfolio.git"

# Initializes git, sets default branch to main, adds remote

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4: Stage and Commit Files
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git add -A
git commit -m "Initial deployment: Professional portfolio for Charan Rachumalla - Senior Platform Engineer"

# Stages all files and creates initial commit

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 5: Push to GitHub
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git push -u origin main

# Pushes code to GitHub and sets upstream tracking

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 6: Enable GitHub Pages
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

gh api --method POST -H "Accept: application/vnd.github+json" "/repos/$(gh api user --jq '.login')/charan-portfolio/pages" -f source[branch]=main -f source[path]=/

# Configures GitHub Pages to serve from main branch root directory

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 7: Get Your Live URL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

echo "Your portfolio is live at: https://$(gh api user --jq '.login').github.io/charan-portfolio/"

# Displays your live portfolio URL

# Copy URL to clipboard (macOS)
echo "https://$(gh api user --jq '.login').github.io/charan-portfolio/" | pbcopy

════════════════════════════════════════════════════════════════════════════════
   📸 ADD PROFILE PHOTO
════════════════════════════════════════════════════════════════════════════════

cp /path/to/your/photo.jpg assets/profile.jpg
git add assets/profile.jpg
git commit -m "Add professional profile photo"
git push

# Adds your photo and deploys update

════════════════════════════════════════════════════════════════════════════════
   🔄 UPDATE WORKFLOW (After Making Changes)
════════════════════════════════════════════════════════════════════════════════

git add -A && git commit -m "Update: [describe changes]" && git push

# One-liner to deploy updates

════════════════════════════════════════════════════════════════════════════════
   🔍 VERIFICATION COMMANDS
════════════════════════════════════════════════════════════════════════════════

# Check if GitHub CLI is authenticated
gh auth status

# View repository details
gh repo view

# Check Pages status
gh api "/repos/$(gh api user --jq '.login')/charan-portfolio/pages" --jq '.html_url'

# Test if site is live
curl -I "https://$(gh api user --jq '.login').github.io/charan-portfolio/"

# Open portfolio in browser
open "https://$(gh api user --jq '.login').github.io/charan-portfolio/"

# Open GitHub repo
gh repo view --web

# Open GitHub Pages settings
open "https://github.com/$(gh api user --jq '.login')/charan-portfolio/settings/pages"

════════════════════════════════════════════════════════════════════════════════
   🛠️ TROUBLESHOOTING COMMANDS
════════════════════════════════════════════════════════════════════════════════

# If not authenticated
gh auth login

# If repository already exists, delete it
gh repo delete charan-portfolio --yes

# Force rebuild deployment
git commit --allow-empty -m "Force rebuild"
git push

# Check git remote
git remote -v

# View recent commits
git log --oneline -5

════════════════════════════════════════════════════════════════════════════════
   ⚡ ULTRA-FAST ONE-LINER (All Steps Combined)
════════════════════════════════════════════════════════════════════════════════

cd /Users/charan/Library/CloudStorage/OneDrive-AlignTechnology,Inc/work_align/kafka_repos/kafka_ops_healthcheck_services/web-portfolio && gh repo create charan-portfolio --public --description "Professional portfolio" && git init && git branch -M main && git remote add origin "https://github.com/$(gh api user --jq '.login')/charan-portfolio.git" && git add -A && git commit -m "Initial deployment" && git push -u origin main && gh api --method POST "/repos/$(gh api user --jq '.login')/charan-portfolio/pages" -f source[branch]=main -f source[path]=/ && echo "Live at: https://$(gh api user --jq '.login').github.io/charan-portfolio/"

# WARNING: Use this only if you understand each command!

════════════════════════════════════════════════════════════════════════════════
   📊 WHAT EACH COMMAND DOES (Technical Explanation)
════════════════════════════════════════════════════════════════════════════════

gh repo create          → Creates GitHub repository via API
git init                → Initializes local git repository
git branch -M main      → Renames default branch to 'main'
git remote add origin   → Links local repo to GitHub remote
git add -A              → Stages all files for commit
git commit -m           → Creates commit with message
git push -u origin main → Pushes code to GitHub, sets upstream
gh api --method POST    → Enables GitHub Pages via GitHub API
echo | pbcopy           → Outputs and copies URL to clipboard

════════════════════════════════════════════════════════════════════════════════
   ✅ SUCCESS INDICATORS
════════════════════════════════════════════════════════════════════════════════

After deployment, you should see:

✓ "Enumerating objects" during git push
✓ "Writing objects: 100%" during git push  
✓ HTTP 201 or success message from Pages API
✓ URL displayed: https://YOUR_USERNAME.github.io/charan-portfolio/
✓ Site accessible after 1-2 minutes

════════════════════════════════════════════════════════════════════════════════
   🎯 NEXT ACTIONS
════════════════════════════════════════════════════════════════════════════════

1. Add profile photo (see commands above)
2. Test on mobile (Chrome DevTools → Toggle device toolbar)
3. Update resume with portfolio URL
4. Add to LinkedIn profile (Contact Info → Website)
5. Share with your network!

════════════════════════════════════════════════════════════════════════════════

Ready to deploy? Copy and paste the commands above!

For fully automated deployment: ./auto-deploy.sh
For manual control: Follow OPTION 2 step-by-step

════════════════════════════════════════════════════════════════════════════════

